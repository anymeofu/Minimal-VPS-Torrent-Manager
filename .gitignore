# Environment variables
.env

# Dependencies
node_modules/

# User-specific data / generated files
downloads/
torrents/
src/db/database.sqlite
src/db/database.sqlite-journal
src/db/sessions.sqlite
src/db/sessions.sqlite-journal

# Build artifacts (if any, besides public/bundle.js which might be versioned or built on deploy)
# For now, assuming public/bundle.js is part of the deployment if not built on server.
# If bundle.js is always built from source on deploy, add:
# public/bundle.js

# OS generated files
.DS_Store
Thumbs.db

# Log files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
