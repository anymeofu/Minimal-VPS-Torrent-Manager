# Credentials for the application
ADMIN_USERNAME=admin
ADMIN_PASSWORD=password123

# Session secret key - VERY IMPORTANT: Change this to a long, random, and unique string.
# Use a password generator (e.g., 64+ random characters) or run in terminal: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your_very_secret_key_for_vps_torrent_manager_CHANGE_ME 

# OMDb API Key for IMDB lookups (optional, but recommended for reliability)
# Get one from https://www.omdbapi.com/apikey.aspx
OMDB_API_KEY=YOUR_OMDB_API_KEY_HERE
